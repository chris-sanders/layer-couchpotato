includes: ['layer:basic','layer:version','layer:hostname','interface:usenet-downloader','interface:plex-info','interface:reverseproxy','layer:backup']  # if you use any interfaces, add them here
repo: git@github.com:chris-sanders/layer-couchpotato.git
options:
  backup:
    backup-name: "couchpotato"
    backup-files: |-
      /home/{couch-user}/.couchpotato/settings.conf
      /home/{couch-user}/.couchpotato/database
